# 공식 감사 보고서 PDF 출력 검증 기록

- 검증일: 2026-08-18 (KST)
- 대상: `audit-trail.html`, `js/audit-trail.js`

| 검증 항목 | 결과 | 확인 내용 |
|---|---|---|
| PDF 파일 생성 | 부분 통과 | 공식 감사 보고서 PDF 파일이 다운로드 경로에 생성됨 (3,058 bytes) |
| PDF 본문 렌더링 | 실패 | 화면 밖으로 이동한 렌더링 스테이지를 변환할 때 1페이지 빈 PDF가 생성됨. 가시 영역 복제본 방식으로 수정 필요 |
| PDF 라이브러리 분리 시험 | 통과 | 화면에 보이는 관리자 헤더를 직접 변환한 PDF 생성 호출이 정상 완료됨. 빈 페이지 원인은 라이브러리 자체가 아니라 보고서 복제본 렌더링 위치·방식으로 판단 |
| 렌더링 스테이지 분리 시험 | 통과 | 가시 영역의 PDF 스테이지에 단순 한글 보고서를 넣어 변환하면 정상 완료됨. 공식 보고서 내부 스타일을 분리·단순화해 재시험 필요 |
| 정적 문서 흐름 분리 시험 | 통과 | 동적으로 만든 보고서라도 일반 문서 흐름에 `position: relative`로 삽입하면 PDF가 정상 생성됨 (9,352 bytes). 고정 위치 스테이지를 정적 흐름으로 변경하기로 결정 |
| 공식 보고서 정적 흐름 재시험 | 실패 | PDF 파일은 37,413 bytes로 생성됐으나 PDF 미리보기·텍스트 추출 모두 본문이 없는 빈 A4 페이지임. 공식 보고서 HTML 구조를 단순화한 별도 PDF 문서 노드 방식으로 재구성 필요 |
| 브라우저 HTML-to-PDF 방식 종합 재확인 | 실패 | 공식 보고서와 단순 정적 테스트 문서 모두 생성 파일이 빈 PDF로 렌더링됨. 라이브러리 기반 직접 다운로드는 제거하고, 브라우저 네이티브 전용 인쇄 창에서 PDF로 저장하는 방식으로 전환 |
| 네이티브 인쇄·PDF 저장 방식 | 성공 | 전용 인쇄 문서에 표지·감사추적 요약·필터 범위·감사 로그가 포함됨을 확인. PDF 저장 버튼이 인쇄 대화상자를 호출하며 브라우저의 `PDF로 저장` 대상 선택으로 심사 제출용 PDF 저장 가능 |
