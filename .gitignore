# 로컬 전용 — 컨설팅 원본 폴더 경로. 공개 배포(GitHub Pages)에 올라가면 안 된다.
data/asset-sources.local.json
data/extract-sources/
data/mdr-rev4-preview.json

# 탐색 및 임시 보고서
_explore-*
_mdr_name_tmp.txt

# 대외 제안용 문서 원본 (오피스 파일)
*.doc
*.docx
*.pptx

# 에디터/OS — 단, .claude/skills/ 는 예외(저장소에 커밋해 다음 세션·다음 사업장에서도
# 쓸 수 있어야 하는 프로젝트 스킬이 들어간다. 로컬 설정/권한 같은 나머지 .claude/ 는 계속 무시)
.claude/*
!.claude/skills/
!.claude/skills/**
desktop.ini
Thumbs.db
.DS_Store
